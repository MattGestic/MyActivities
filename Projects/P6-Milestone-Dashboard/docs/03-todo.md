# P6 Milestone Dashboard — To-Do

Short-lived, tactical. Bugs, blockers, immediate next actions. Close items out rather than archiving them here.

| ID | Item | Linked | Priority | Status | Raised |
|---|---|---|---|---|---|
| TD-01 | Version string mismatch: the migrated file was supplied as `..._v3_1_0_11.html` but `APP_VERSION` reads `3.1.0-P1`. | FEAT-15 | H | **Closed 2026-09-09** — confirmed `3.1.0-P1` is correct. The `_11` in the supplied filename was a save-copy suffix, not a partial number. `APP_VERSION` unchanged, commit tagged `v3.1.0-P1`. | 2026-09-09 |
| TD-02 | Blank Data-date / Report-date on `.xlsx` import. Root cause never confirmed; a "not set" fallback is in place. | FEAT-02 | M | Open (blocked) — **did not recur** under TEST-02 AC-08 on the 29-Aug export: renders `29-Aug-26`, fallback not triggered. That is one file passing, not a root cause. Still needs the original triggering file. | Pre-migration |
| TD-03 | Companion docs referenced by the handoff were not supplied. | TASK-04, FEAT-14 | H | **Closed 2026-09-09** — `Token_Migration_Log.md` and `Tokenization_Path_Plan.md` supplied and committed to `docs/tokenization/`. `Hardcoded_Colour_Audit.csv` was not supplied but is a generated artefact: regenerated from the documented method by the now-committed `tools/colour_audit.py`. FEAT-14 unblocked. | 2026-09-09 |
| TD-06 | Audit script v1 under-reported the tokenization backlog. It matched hex only (never `rgb()`/`rgba()`), and counted the 18 `--space-*` definitions as references. Real remaining work is larger than the plan assumed: 131 colour occurrences not 99, and 173 raw px spacing declarations not 117. Re-scope FEAT-14 phases against the v2 rows in the Measurement Log before starting any phase. | FEAT-14, TASK-05, TASK-06 | H | Open | 2026-09-09 |
| TD-07 | Hardcoded `rgba()` occurrences never triaged, because audit v1 could not see them. | FEAT-14 | M | **Partially closed 2026-09-09** — the `rgba()` values in the dependency tooltip and comment panel were theme-blind and are now tokenized. The remainder (shadow washes and pale tints with no token match) still need Phase 2 triage. Tracked under TASK-12. | 2026-09-09 |
| TD-08 | Re-check the four component rows in the Token Migration Log still dated `2026-09-04`. | FEAT-14 | M | **Closed 2026-09-10** — marker icon states, subtotal row and remarks field states all tokenized and re-dated. Discipline band rows re-checked, still untokenized, carried forward as TD-14. | 2026-09-09 |
| TD-10 | The `<title>` carried a hardcoded version and never read `APP_VERSION`, breaking the single-source-of-truth invariant. TEST-03 had recorded this as passing; the assertion only matched by coincidence. | FEAT-15 | H | **Closed 2026-09-09** — title and static label text now derive from `APP_VERSION` at load. Exactly one version literal remains in the file, and that is now the asserted condition. | 2026-09-09 |
| TD-11 | Two latent CSS defects repaired during the v3.1.0-P2 pass: `--color-border-panel` was self-referential in the dark block (invalid cycle, every dark border reading it fell back), and `tr.subtotal-row td.c-wk.past-col` carried a malformed `color:var(--color-text-note)9cc` the parser dropped. Both look like a botched earlier find-and-replace. | FEAT-14 | H | **Closed 2026-09-09** | 2026-09-09 |
| TD-12 | The colour audit scanned the `<style>` block only, so inline `style=` colours were invisible. | FEAT-14 | M | **Closed 2026-09-10** — audit extended to inline `style=` attributes with a `source` column. Immediately found a theme-blind `#c00000` in JS-generated diagnostics markup, now tokenized. | 2026-09-09 |
| TD-13 | Hover and focus states are not probed by `theme_check.py`, so `tr.data:hover`, `.remarks:focus` and similar are tokenized but unverified at runtime. Needs a probe that forces the state rather than relying on real interaction. | FEAT-14 | M | Open | 2026-09-10 |
| TD-14 | Discipline band rows are the last board component still untokenized. Left alone this pass because their role and value set were not clear from the CSS alone. | FEAT-14 | M | Open | 2026-09-10 |
| TD-09 | Verify the three CSS traps in `06-lessons-learned.md` §4 are still correctly handled in the current file: `border-collapse: separate` for sticky table cells, `min-width: 0` on wrapping flex children, and no `innerHTML` rebuild of a clicked element's own container without `stopPropagation()`. These were fixed pre-migration; this is a guard against reintroduction, not a re-verification of TEST-01. | — | L | Open | 2026-09-09 |
| TD-04 | Run the 29-Aug-2026 export through import as a live ingest test. | TEST-02, FEAT-02 | H | **Closed 2026-09-10** — TEST-02 run via `tools/import_check.py`. 8 criteria pass, 1 fail (TD-15), 1 not testable without network (AC-01), 1 criterion was itself wrong (TD-17). Hierarchy detection exact at 146/46. | 2026-09-09 |
| TD-15 | The finish-date constrained flag is discarded. `parseLooseDate` detects a trailing `*` on both Start and Finish, but `normalise` records only `startStarred`; `fin.starred` is parsed and dropped. One cell in the reference export (`E13`, `11-Sep-26*`) loses its marker. Design gap, not a regression: the star was only ever specified for start dates. Decide whether a constrained finish should carry a marker, and what consumes it, before adding the field. | FEAT-02, TEST-02 | M | Open | 2026-09-10 |
| TD-20 | The stage-chain row rule did nothing for EPCM/construction schedules. | FEAT-02, FEAT-10 | **H** | **Closed 2026-09-10** (v3.1.0-P5) — mixed strategies selected per band: tag, system, area, stage, activity. EPCM 2857 activities to 1948 rows; PFS unchanged at 115. | 2026-09-10 |
| TD-25 | Stage phrases, discipline terms and system terms are fixed lists. `deliverableStem` also assumes the phrase is a suffix; a schedule that puts the stage in the middle of a name would not match. Reported failures were fixed by leaning on dependencies rather than names, which limits the exposure, but the lists are still project-shaped. | FEAT-02 | M | Open | 2026-09-10 |
| TD-26 | A convergence node always stood alone, even when it was name-compatible with exactly one of its candidate rows. `SNIP-189` "Site Plan -Client Review" separated on two in-band predecessors. | FEAT-03 | M | **Closed 2026-09-10** (v3.1.0-P8) — standalone now requires 6+ total predecessors or 30%+ from outside the band. 6 rather than 5 because `SNIP-161` sits at exactly 5 and belongs in the MEL chain; distribution and reasoning in TEST-13. `SNIP-189` now merges. PFS 108 to 105 rows, 8/8 guards hold. | 2026-09-10 |
| TD-23 | Sub-headings within a band are not built. The agreed rule was "either subheadings or rows for the different systems or locations, depending on numbers"; only the row half exists. 1769 of 1948 EPCM rows still carry a single marker, concentrated in the 208 bands that fall back to one row per activity. | FEAT-10, FEAT-03 | **H** | Open | 2026-09-10 |
| TD-24 | `disciplineTerms` and `systemTerms` are fixed lists in `INGEST_CONFIG`. They fit Eskay Creek; another project's WBS vocabulary would need editing them. Consider deriving them from the imported WBS instead. | FEAT-02 | M | Open | 2026-09-10 |
| TD-21 | XER ingest is not in the application. `tools/xer_to_aoa.py` converts an XER to the array-of-arrays the `.xlsx` path produces, which is enough to test with but not a user-facing feature. Wanted as a future inclusion. | FEAT-02 | M | Open | 2026-09-10 |
| TD-22 | Hour-to-day conversion in the XER extractor assumes an 8 hour day for durations and total float. P6 calendars can differ per activity; confirm before any in-app XER ingest relies on it. | TD-21 | L | Open | 2026-09-10 |
| TD-16 | Dependency data is never refreshed by an import. `DEP_DATA` is a baked-in constant from the 22-Aug-26 export; against the 29-Aug import, 106 of its 143 activity IDs are absent, so dependency lines describe a superseded logic network. | FEAT-04, FEAT-02 | **H** | **Half closed 2026-09-10** (v3.1.0-P4) — ingest done: the mapper exposes Predecessor/Successor, both auto-map, and `normalise` carries them onto each activity. Still open: the rendering layer reads `DEP_DATA` rather than the imported relationships. | 2026-09-10 |
| TD-17 | AC-10 in `05-test-log.md` asserted that dependency lines are drawn automatically after import. They are opt-in per milestone, confirmed by measuring the baseline with no import at all (also zero lines, `DEP_VIS` empty). Restated as AC-10r. No code defect. | TEST-02 | L | **Closed 2026-09-10** | 2026-09-10 |
| TD-18 | The 29-Aug import formed only 38 deliverable rows from 146 activities. | FEAT-02 | M | **Closed 2026-09-10** — two parts. No data was being lost: `minGroupSize` is 1 and every activity was retained; the earlier suspicion was unfounded. The shape was wrong though, because grouping keyed on the whole WBS band. The v3.1.0-P4 row model fixes it: 115 rows, one per deliverable. | 2026-09-10 |
| TD-27 | A milestone moved between rows by drag is display state only. `MS_MOVES` records it and the export payload does not carry it, so a re-import rebuilds the model and the move is not replayed. | FEAT-03, FEAT-08 | M | **Closed 2026-09-11** (v3.1.0-P20) — decided in favour of round-tripping. A move is now recorded against the milestone's identity rather than the key it has afterwards, which is what made replay possible: `msKeyFor` falls back to a composite embedding the row ref, and that is exactly what a move changes, so replaying by the post-move key could never match a freshly imported milestone sitting on its original row. Carried in both payloads and replayed by the `msMoves` import category. | 2026-09-10 |
| TD-28 | The WCAG contrast check skipped any element with a transparent background, so text sitting directly on a row was never measured. `.row-num` shipped into review at 1.99:1 in dark before this was found. The checker now walks to the nearest painting ancestor. Re-run against every existing transparent-background component: `--color-text-small` measures **1.18:1** on a dark row, so anything using it outside its own backing is exposed. | FEAT-14 | **H** | Open | 2026-09-10 |
| TD-29 | Row dragging is not built. The row number is rendered as a flex slot specifically so the drag handle can join it, and `data-rownum` is already on each `tr.data`, but reordering rows is the next stage and does not exist yet. | FEAT-10, FEAT-11 | M | Open | 2026-09-10 |
| TD-30 | `LAST_MARKUP_AT` was assigned but never declared, so it existed only as an implicit global after the first markup edit. `exportModel()` reads it, so **exporting before touching anything threw a ReferenceError and the download silently never happened**. Present since before the migration; never caught because every manual test had made an edit first. | FEAT-08 | **H** | **Closed 2026-09-10** (v3.1.0-P9) — declared alongside `MARKUP_COUNT`. Found by round-tripping an export through the new validator on a clean load, not by reading. | 2026-09-10 |
| TD-31 | Unmounting an annotations file disconnects the file but does not roll back what was imported from it. Deliberate: once imported, those values are edited in place and are indistinguishable from anything typed by hand, so a rollback would have to guess. The confirm dialog says so. Revisit if per-source provenance is ever tracked. | FEAT-13 | L | Open | 2026-09-10 |
| TD-32 | Round-trip import covers the annotation layer only. `tasks`, `milestones` and `timeline` are in the export payload but a mounted model does not rebuild the board from them — the schedule slot still has to be fed a P6 export. Decide whether a model file should be able to stand in for the schedule entirely. | FEAT-13 | M | Open | 2026-09-10 |
| TD-33 | `--color-text-small` measures **1.18:1** on a dark row and `--color-purple-dark` **1.13:1** on the accent wash. Both were walked into on the first pass of the mount panel despite TD-28 naming the first one, and both were caught by probes rather than review. Every other consumer of these two tokens still needs checking. | FEAT-14 | **H** | Open | 2026-09-10 |
| TD-34 | **A failed file load left the previous file's column mapping and Import button on screen.** Pick a good file, then a bad one, and the board still offered to Import — the earlier file. Worse than no button: it imports the wrong schedule while naming the right one. | FEAT-02 | **H** | **Closed 2026-09-10** (v3.1.0-P10) — every failure path now routes through `importFailed()`, which clears `LAST_PARSE` and the mapper before showing the error. | 2026-09-10 |
| TD-35 | A failed `.xlsx` load was reported only by the 9.5px italic status line, and step "2 · Map columns" simply never appeared. Its own hint reads "Appears once a file loads", so the absence read as a broken Import button rather than as an error with a cause. Reported by the user as "the import button doesn't show when a file is uploaded". | FEAT-02 | **H** | **Closed 2026-09-10** (v3.1.0-P10) — a failure block now occupies the space where step 2 would have been, naming the file, the reason and the remedy, with actions to jump to the paste box or pick another file. | 2026-09-10 |
| TD-36 | (Note: this was **not** the cause of the user's reported failure — SheetJS loaded fine there; see TD-37.) `.xlsx` import depends on fetching SheetJS from cdnjs at first use. On a locked-down corporate network, offline, or a `file://` page under a strict policy, Excel import is impossible and only CSV/paste work. This is the single external dependency in the app and the only thing that can make a whole input format unavailable. Options: bundle a minimal XLSX reader (an xlsx is a zip of XML, so this is real work), inline SheetJS (roughly triples file size), or accept it and keep the CSV fallback prominent. | FEAT-02, FEAT-12 | **H** | Open | 2026-09-10 |
| TD-37 | The P10 failure block reported "The file could not be read from disk" and **discarded the `DOMException` that names the actual cause**. Reported by the user with a screenshot of exactly that message on a real `.xlsx`. The failure was visible but not diagnosable, which is only half the fix. It also withheld the paste-box action on this path, wrongly: a file that cannot be READ says nothing about whether the app can handle the data. | FEAT-02 | **H** | **Closed 2026-09-10** (v3.1.0-P11) — `readerErrorDetail()` names `NotReadableError` / `NotFoundError` / `SecurityError` and gives the matching remedies; paste is offered on this path. | 2026-09-10 |
| TD-38 | A `FileReader` failure was reported on the first attempt. A cloud-only OneDrive placeholder hydrating, or a brief antivirus lock, both clear within a moment. | FEAT-02 | M | **Closed 2026-09-10** (v3.1.0-P11) — `readFileWithRetry()` makes one silent second attempt 400ms later. Verified: a failure that clears on the retry recovers with no error shown and the Import button appearing normally. | 2026-09-10 |
| TD-39 | The data date lived in Advanced Input Settings → Parsing options, which is **hidden until a file loads**, and was labelled "Data date (schedule as-of)". It is set on every import, not rarely, so it belonged with the other per-import fields. Its default was also the hardcoded literal `2026-07-29`, which went stale the moment the baseline moved. | FEAT-02, FEAT-07 | M | **Closed 2026-09-10** (v3.1.0-P12) — moved under "1 · Import a Schedule" above Report date, relabelled "Data date", default computed as the previous Friday at load. | 2026-09-10 |
| TD-40 | **A successful import left the "Ready to import / Discard / Import" panel on screen.** `runIngest()` hid the mapper and nulled `LAST_PARSE` but never cleared `import-summary-wrap`; `cancelIngest()` did. A finished import therefore looked unfinished, and the obvious response — press Import again — did nothing, because `LAST_PARSE` had just been nulled. Reported by the user as "Import button doesn't click"; the first press had in fact worked. | FEAT-02 | **H** | **Closed 2026-09-10** (v3.1.0-P13) — the success path clears the form and any failure block. `runIngest()` with nothing staged now says whether a file is already mounted instead of "Nothing parsed yet." | 2026-09-10 |
| TD-41 | `const MS_COMMENTS={}` was declared **after** the init block, so anything init touched it from hit a temporal dead zone. Publishing did exactly that: `applyPublishedState()` threw partway, silently falling back to the seeds for everything after that line while the earlier half had already applied. Second instance of the class TD-30 raised. | FEAT-02 | **H** | **Closed 2026-09-10** (v3.1.0-P14) — moved up beside `MS_SHORT_TITLES` and `MS_HEALTH_OVERRIDE`, where it belonged. | 2026-09-10 |
| TD-42 | `publishDashboard()` cloned every `script` element in the DOM, so anything injected by a browser extension, a document viewer wrapper (the `about:srcdoc` preview does inject), or a debugging snippet would be **baked into a file uploaded to SharePoint and opened by other people**. Found when a test harness's own script was cloned into a published file and re-ran on load, overwriting the page title. | FEAT-02, FEAT-12 | **H** | **Closed 2026-09-10** (v3.1.0-P14) — the app's script carries `id="app-script"`; publish drops every other script and logs how many. | 2026-09-10 |
| TD-43 | A published file still carries the original `SEED_TASKS` / `SEED_MILESTONES` blocks as a fallback beneath the state block, so it is larger than it needs to be (530 KB against a 435 KB source, of which ~94 KB is the state block). Deliberate for now: the seeds are the fallback if the state block is ever malformed. Revisit if published size becomes a problem on SharePoint. | FEAT-12 | L | Open | 2026-09-10 |
| TD-44 | The published file's own "Save as new dashboard" button still works, so a reader can republish from a published file. Chained publishes are handled (the previous state block is replaced, not stacked) but the provenance chain was not recorded beyond `fromVersion`. | FEAT-12 | M | **Closed 2026-09-11** (v3.1.0-P21) — decided: readers **should** be able to republish. `publishChain` now records one entry per publish, oldest first, and the Schedule slot says how many republishes a file has been through and when it was first published. A file published before the chain existed gets a one-entry chain synthesised from its stamp. | 2026-09-11 |
| TD-45 | `--color-bg-header` moved from `#ffffff` to `#a6bbdc` on request. The token feeds seven surfaces, and `--color-text-on-panel-muted` sits on **two different backgrounds** (panel and header), so the change dropped the sticky search icon and clear button to **2.40:1**. `tr.hist-row` shares the same pair and had no probe at all — it would have gone unnoticed. Token darkened to `#575c67` (3.44:1 header, 6.15:1 panel) and a `tr.hist-row` probe added. | FEAT-14 | M | **Closed 2026-09-11** (v3.1.0-P15) | 2026-09-11 |
| TD-46 | A token used on more than one background is a latent trap: changing either background silently breaks the pairing, and the contrast check only catches it where a probe exists. | FEAT-14 | M | **Closed 2026-09-11** (v3.1.0-P21) — proved by TD-50: darkening the header dropped `--color-text-on-panel-muted` to **1.22:1**. Header surfaces now use `--color-text-on-header-muted` and the panel token is single-surface again. The probe rule is now per text colour landing on a background, not per component someone remembers. | 2026-09-11 |
| TD-47 | **A published file misreported itself, so a correct publish looked like a failed one.** The board held the published 105 rows, but the mount panel read `SEED_*` for the baseline counts and showed "159 deliverables / 198 milestones"; the header said "Baseline: Baseline DD 15th Aug" (a `const` the publish path never touched) and "Schedule update: No update imported"; the Schedule slot said "Nothing mounted / Showing the embedded baseline". Every one of those is literally true of the internals and wrong about what the user did. Reported as "the current I had loaded didn't save into the copy" — it had saved. | FEAT-12, FEAT-17 | **H** | **Closed 2026-09-11** (v3.1.0-P16) — counts come from `BASELINE_*`; the slot badges as `published` and states when and from which build; the header reads "Published DD <date>" and "Built into this file"; the empty Schedule slot explains that it is the normal state. | 2026-09-11 |
| TD-48 | TEST-19 asserted the published file's data and annotations but never what its UI said about them, which is why TD-47 shipped. Any feature that changes a file's provenance needs the provenance display asserted, not just the payload. | FEAT-12 | M | **Closed 2026-09-11** (v3.1.0-P16) — the publish probe now captures all three mount slots and the header meta line. | 2026-09-11 |
| TD-49 | The header meta row is reordered to Project No, Report date, Data date, Activities, Baseline, Updated by. **Data date was never displayed**: `updateHeaderMeta()` wrote to `#meta-datadate`, but no such element existed in the markup, so the write was dead — and its value was the now-column label ("W/E 13-Sep") rather than the schedule's data date, which is a different thing. Now a real field showing `DATA_SOURCE.dataDate`. "Schedule update" left the header; the Mounted Sources panel states the same thing with more detail. | FEAT-07 | M | **Closed 2026-09-11** (v3.1.0-P17) | 2026-09-11 |
| TD-50 | **`#f4f5f8` on the light header measured 1.79:1.** Requested for the icon-bar version label and the report subtitle, applied as asked, and the contrast gate was left failing rather than suppressed. | FEAT-14 | **H** | **Closed 2026-09-11** (v3.1.0-P21) — resolved the way the user chose: `--color-bg-header` darkened from `#a6bbdc` to `#2e4f82`, which takes the requested colour to 7.53:1 and leaves the dark header untouched. Darkening moved every other text colour on that surface, so it came with the TD-46 split below. | 2026-09-11 |
| TD-51 | A published schedule installed itself as the **baseline**, which destroyed the one thing the baseline exists for: comparison against the original embedded dataset. A reader of a published file had no baseline to toggle to and no overlay. It now installs as the **update**, with the embedded baseline intact behind it. | FEAT-12 | **H** | **Closed 2026-09-11** (v3.1.0-P18) | 2026-09-11 |
| TD-52 | The Source cell lists the activity IDs behind a row and was built once at aggregate time, so dragging a milestone between rows left both rows describing the pre-drag grouping. Both affected rows are now rebuilt from what they actually hold. | FEAT-03 | M | **Closed 2026-09-11** (v3.1.0-P18) | 2026-09-11 |
| TD-53 | Dragging the last milestone out of a row left an empty row with no way to remove it. A delete control now appears on rows holding no milestones; it re-checks emptiness on click, because the debounced rerender means the board a click was aimed at can be one state behind. | FEAT-03 | M | **Closed 2026-09-11** (v3.1.0-P18) | 2026-09-11 |
| TD-54 | "Hide All Columns" also stripped the labels off every milestone marker, because `btn-lbl` and `btn-mhrs` were in its id list. Those govern what is drawn on the board, not which table columns exist. Removed from the master toggle. | FEAT-07 | M | **Closed 2026-09-11** (v3.1.0-P18) | 2026-09-11 |
| TD-55 | Row number and health dot were loose inline elements, so deliverable names did not line up between single and triple digit row numbers and the two could collide. Both now sit in a fixed-width `.row-gutter` flex slot, which is also where the delete control and the future row-drag handle go. | FEAT-03 | L | **Closed 2026-09-11** (v3.1.0-P18) | 2026-09-11 |
| TD-56 | Heading restructured: one bar holding the report title on the left and the report detail fields right-justified, wrapping to stacked containers on narrow widths; the subtitle moved beneath the whole bar rather than under the title; the separate `.rpt-sub` bar folded into the heading's right container. Subtitle text replaced. **Assumption flagged:** the requested wording was read from a partly garbled dictation ("Bert says XYZ …") as "Exported milestone view of P6 schedule shown by activity end dates". Easy to correct if that is wrong. | FEAT-07 | M | **Closed 2026-09-11** (v3.1.0-P19) | 2026-09-11 |
| TD-57 | The search box shared the month band's line from inside the sticky corner cell. It now has its own `tr.hdr-search` row at the top of the table head, so it keeps the sticky behaviour the corner cell gave it. The three header rows' sticky offsets are driven by one `--hdr-search-h` variable rather than the hardcoded `top:19.5px`, so they cannot drift apart if the search row's height changes. | FEAT-07, FEAT-06 | M | **Closed 2026-09-11** (v3.1.0-P19) | 2026-09-11 |
| TD-58 | **Dependency-line comments reached neither payload.** `DEP_COMMENTS` was absent from `publishStatePayload()` and from `exportModel()`, so the one kind of comment that hangs off a dependency line rather than a milestone was silently dropped by both Save as new dashboard and the model export. Every other annotation store was carried. | FEAT-12, FEAT-13 | **H** | **Closed 2026-09-11** (v3.1.0-P20) — in both payloads, restored on load, and selectable on import as its own category. | 2026-09-11 |
| TD-59 | **A published file never applied its own row health and remarks on first paint.** `applyPublishedState()` parks them in `PENDING_ROW_OVERRIDES` because they key on rendered rows; `rerender()` drains that queue but the init path builds the first paint through `renderRows()` directly and did not. The values were in the file and correct, and appeared only once something else happened to trigger a rebuild. Third instance of two paths that must do the same thing drifting apart (TD-40, TD-47). | FEAT-12 | **H** | **Closed 2026-09-11** (v3.1.0-P20) — init drains the queue in the same position `rerender()` does. | 2026-09-11 |
| TD-60 | Row removals now round-trip alongside milestone moves. A replayed removal declines the same case the interactive control declines: a row that has come back with milestones on it is left alone, so the import cannot be used to get around the emptiness check. | FEAT-03, FEAT-13 | M | **Closed 2026-09-11** (v3.1.0-P20) | 2026-09-11 |
| TD-61 | A published file reported "no markup edits yet" regardless of how much markup was baked into it, because the counters were not in the payload. Same class as TD-47: the data was right and the thing describing it was wrong. | FEAT-12 | M | **Closed 2026-09-11** (v3.1.0-P20) | 2026-09-11 |
| TD-62 | The two replayed import categories change the board rather than merging into a store, so they are the only categories that can partly decline: a move whose milestone is absent from the current schedule, or a removal whose row is no longer empty, is skipped silently. The import summary reports the category as applied without saying how many entries actually landed. | FEAT-13 | M | Open | 2026-09-11 |
| TD-63 | The data date default returned the *previous* Friday even when today was a Friday, which was recorded as by-design in TEST-17 and queried with the user. It is wrong: a data date is set on the day the export is taken, and the week that just closed is the one being reported. Now returns today when today is a Friday. `previousFriday` renamed to `mostRecentFriday`, since the old name described the old rule. | FEAT-02, FEAT-07 | M | **Closed 2026-09-11** (v3.1.0-P21) | 2026-09-11 |
| TD-64 | **A background token was being used as a text colour.** `.sd-src-box b` and `.sd-src-text b` coloured bold values with `--color-bg-header`. It read as blue emphasis in light by luck, and measured **1.79:1** at `#a6bbdc`; in dark it was the dark navy header on a dark panel at **1.31:1**. Pre-existing, never probed, and found only because the TD-46 sweep added a probe for every consumer of the header token including the ones using it as text. Now `--color-text-emphasis`, themed per surface. | FEAT-14 | **H** | **Closed 2026-09-11** (v3.1.0-P21) | 2026-09-11 |
| TD-65 | **An imported board presented its bands in alphabetical order, not the schedule's.** `aggregate()` builds `bandOrder` in file order and then threw it away, sorting groups on the WBS path STRING. So "Capital and Operating Cost Estimate" led the board because it starts with C, and the Key Milestones the schedule opens with landed fourth. Reported by the user against the Aug-29 import, with the source workbook alongside. Present for as long as import has existed; the baseline seeds carry no WBS so the shipped board never showed it. | FEAT-02, FEAT-10 | **H** | **Closed 2026-09-14** (v3.1.0-P22) — groups carry their band's file position and their row position within it, and the sort uses those. | 2026-09-14 |
| TD-66 | The first version of the TEST-25 probe derived its expected order by looking for rows with an empty Activity ID column, which is the opposite of this export's shape. It found nothing, printed an empty expected list, and let the run fall through to a weaker id-monotonicity heuristic that passed. A check that derives its own expectation must fail when the derivation comes back empty, never continue on a fallback. | FEAT-16 | M | **Closed 2026-09-14** (v3.1.0-P22) — the derivation is fixed and an empty result now exits with an error rather than passing vacuously. | 2026-09-14 |
| TD-67 | The schedule nests below the band the board renders: Engineering holds Process, which holds Design Criteria, PFD, Mass and Water Balance and Equipment Sizing. Those inner headings collapse into the discipline band and are not shown, so a reader cannot see the grouping the schedule uses. Related to TD-23. Raised by the same report that produced TD-65. | FEAT-10 | M | Open | 2026-09-14 |
| TD-68 | Ten presentation and default changes requested together: drop the derived-weights remark from generated rows, open with columns collapsed and milestone labels off, move View Controls to the right with its launcher beside Settings, app name and version to the top right, report detail fields under the subtitle, comments icon fitted to its button, row gutter spacing with remarks aligned to the row title, new subtitle wording, predecessor and dependency ID lists in the milestone card, and the search moved inline with the other filters. | FEAT-07, FEAT-03 | M | **Closed 2026-09-14** (v3.1.0-P23) | 2026-09-14 |
| TD-69 | **Moving the View Controls panel to the right left the page pushed the wrong way.** `body.cv-open` applied `margin-left:300px`, correct while the panel was on the left. With the panel on the right the board shifted away from it and the panel sat off the edge of the viewport. A panel's open-state page offset is part of the panel, not of the page, and has to move with it. | FEAT-07 | **H** | **Closed 2026-09-14** (v3.1.0-P23) | 2026-09-14 |
| TD-70 | `.remarks` set `margin-left` twice in the same rule: the new gutter indent first, then the original `-3px`. The later declaration won and the indent silently did nothing, so the fix read as applied and measured as absent. Same-property duplicates inside one rule are invisible to a diff. | FEAT-07 | M | **Closed 2026-09-14** (v3.1.0-P23) | 2026-09-14 |
| TD-71 | The derived-weights remark was also baked into every seed row, so clearing it from the ingest path left it on screen for the shipped baseline. A change to what the app generates has to be applied to the generated data already committed, or only new imports get it. | FEAT-02 | M | **Closed 2026-09-14** (v3.1.0-P23) | 2026-09-14 |
| TD-72 | The dependency tooltip and the notes dialog listed only the activity IDs, joined on one line (`#SNIP-127 | #SNIP-166`), so a reader had to know the IDs by heart to know what the line connected. Both now show one row per activity as `#ID: Title`, a step smaller, with the title truncated to the row rather than wrapping the box. The row builder is shared between the two so the formats cannot drift. What lands on the clipboard on click is unchanged. | FEAT-04, FEAT-05 | M | **Closed 2026-09-14** (v3.1.0-P24) | 2026-09-14 |
| TD-73 | **Future:** make the activity ID in the dependency tooltip and notes dialog a navigation link that scrolls to and highlights that milestone on the board. Raised with TD-72 and deliberately not built there. Needs a decision on what "navigate" does when the target is filtered out of the current view, or sits in a collapsed band. | FEAT-04, FEAT-06 | M | Open | 2026-09-14 |
| TD-74 | The report title moved from the heading block into the top row alongside the app name and icons, a step larger (12px to 13px). It truncates rather than wraps, because the bar is one row, and expands while being edited. | FEAT-07 | M | **Closed 2026-09-14** (v3.1.0-P25) | 2026-09-14 |
| TD-75 | Deliverable names wrapped, so every row was a different height. Now one line with an ellipsis by default, with an **Allow activity title to wrap** toggle at the end of the View Controls Layout section. Display state only; it never touches the schedule or the annotation layer. | FEAT-07 | M | **Closed 2026-09-14** (v3.1.0-P25) | 2026-09-14 |
| TD-76 | **A marker could read as belonging to the row above or below it.** Two causes, both fixed. Markers were laid out inline, so two in one cell sat side by side as a centred pair with neither on the cell's midline. And the label-collision band moved the whole `.m-wrap` by 15px, carrying the icon with it, which on a 34px row put it most of the way to the boundary. Placement is now a property of the cell: `--mx`/`--my` set the centre as a percentage, one marker is dead centre, several spread along x and alternate above and below, and every centre stays at least 10% clear of each boundary. The band now moves the label only. | FEAT-03, FEAT-06 | **H** | **Closed 2026-09-14** (v3.1.0-P25) | 2026-09-14 |
| TD-77 | Markers no longer sit in the cell's flow, so they stopped contributing its height. A zero-width strut per occupied cell restores it. If marker rendering ever changes again, that strut is what keeps row heights from collapsing. | FEAT-03 | L | **Closed 2026-09-14** (v3.1.0-P25) | 2026-09-14 |
| TD-78 | Three or more markers in one cell overlap slightly at the default 36px week column: three 15px icons cannot fit a 36px cell without it, whatever the arrangement. Measured as clean at a 60px column, so the Week col slider is the remedy. The alternative, widening the vertical alternation, would push icons past the row boundary and reintroduce TD-76. Revisit only if crowded cells become common. | FEAT-03 | L | Open | 2026-09-14 |
| TD-79 | The week filter washed every body cell in the range and framed it with purple borders, which buried the markers the filter exists to show. The highlight now lives on the heading only. `.filter-col` is still applied to the cells, because fit-to-screen reads it to know which columns are in play; it simply paints nothing. | FEAT-06 | M | **Closed 2026-09-14** (v3.1.0-P26) | 2026-09-14 |
| TD-80 | The month band was never highlighted by the week filter, only the week row was. A month covering any filtered week now highlights too, in white on the accent. The rule has to win on specificity and restate the colour, because each month sets its own background inline and that colour is unrelated to the accent. | FEAT-06 | M | **Closed 2026-09-14** (v3.1.0-P26) | 2026-09-14 |
| TD-81 | Milestone card reordered: the parent heading moves above the title so the band reads before the milestone is named, the short title field moves under the title, and total float becomes its own column beside both, two rows deep, with the label at the short title's size. Float was previously a tag stuck on the end of the title. | FEAT-05 | M | **Closed 2026-09-14** (v3.1.0-P26) | 2026-09-14 |
| TD-82 | **The Show dependency counts toggle duplicated the whole board on every use.** `onCountsToggle()` called `renderRows()` directly, but `renderRows()` appends rows and never clears; the only thing that empties the tbody is `teardown()`, which runs inside `rerender()`. Each toggle therefore built a second copy of all 159 rows underneath the first. The stale copy carried no chips and the fresh one did, both on screen at once, so turning counts OFF appeared to do nothing: the chips that stayed belonged to the copy the toggle had just added. Reported as "the show dependency count button isn't working". Now goes through `scheduleRerender(true)` like every other user action needing a rebuild, which is what CLAUDE.md already required. | FEAT-04, FEAT-07 | **H** | **Closed 2026-09-15** (v3.1.0-P27) | 2026-09-15 |
| TD-83 | **The dependency counts were rendered but not readable.** 8px unbacked digits pinned `bottom:-9px`, i.e. fully outside the icon and sitting on the boundary between two rows, with the label stack (which starts at the same `left:100%`) covering the right-hand one. Measured at 4.5 x 8px. They are now backed chips at the icon's own mid-height, flanking it, in the two colours the dependency lines use, and the label stack is pushed clear of the right-hand chip while counts are on. An element that is present and measurable is not therefore legible; position and overlap need asserting, not existence. | FEAT-04 | M | **Closed 2026-09-15** (v3.1.0-P27) | 2026-09-15 |
| TD-84 | Baseline overlay corrected against the stated rule. It copied the live marker's `pos.x` into a different cell, carrying that cell's crowding offset for no reason, and `.m-ghost` then nudged the ghost 3px down so the pair was never level. X now comes from the ghost's own baseline date, Y from its pair, and `z-index:0` puts it behind every live marker and label. An unmoved baseline is nudged in X off its **pair's** position, not off the cell centre: centring it was not enough, because a cell holding several markers spreads them and the ghost landed under one. The tooltip named a hardcoded "29-Jul-26" regardless of the actual baseline; it now reads `BASELINE_LABEL_TEXT` and states both dates and the movement. Each ghost carries `data-ghost-for` naming its pair, since two markers in a row can share a baseline column and the pairing cannot be recovered from position. | FEAT-09, FEAT-06 | M | **Closed 2026-09-15** (v3.1.0-P27) | 2026-09-15 |
| TD-85 | The Show baseline schedule toggle sat inside the Dependencies section of View Controls, paired with Show dependency counts, which it has nothing to do with. Moved to its own Baseline section with a note stating what it matches on and how it draws. | FEAT-07, FEAT-09 | L | **Closed 2026-09-15** (v3.1.0-P27) | 2026-09-15 |
| TD-86 | A3 portrait print preview added to the icon bar as a toggle. It holds the board at the sheet's printable width, fits the week columns to it through the same `fitColumnsToWidth()` the fit-to-screen button uses, closes the View Controls panel (whose 300px body margin would push the page frame off centre) and restores it on the way out, and injects the `@page` rule only while the preview is on so a plain Ctrl+P is unaffected. The preview never travels into a published file. Where the columns cannot fit even at the 20px floor, the banner says so rather than silently clipping. | FEAT-07 | M | **Closed 2026-09-15** (v3.1.0-P27) | 2026-09-15 |
| TD-87 | Two assertions in the first TEST-30 run passed against an empty set: the chip measurements ran before the debounced rerender had rebuilt the board, so "every chip is level" was true of zero chips. Third occurrence of a vacuous pass in this project's probes (TD-66, and the P26 float column). Every probe that measures a set now asserts its sample size first. | FEAT-16 | M | **Closed 2026-09-15** (v3.1.0-P27) | 2026-09-15 |
| TD-88 | `--color-bg-subtle` is declared twice inside the dark theme block: `#232f4a` then `#f4f5f8`, a light value, which wins. Pre-existing, found while choosing a background for the print preview and deliberately not changed there. Same class as TD-70, a same-property duplicate inside one block that a diff cannot see. Needs a sweep of its consumers before the second declaration is removed, since some may have been tuned against the light value. | FEAT-14 | M | Open | 2026-09-15 |
| TD-89 | **The board's span had nothing to do with the schedule being imported.** `buildTimeline()` was always called as data date minus 12 weeks to data date plus 26, a fixed window inherited from the seeds. On the Aug-29 export that lost three milestones off the end (out of horizon, plotted nowhere, a diagnostic only) and padded eight empty weeks onto the front. The base range now comes from the data: `deriveScheduleRange()` reads both ends of every milestone and task span, and `buildTimelineFromRange()` builds the weeks that enclose it. Measured against the workbook independently of the app: 01-May-26 to 26-Nov-26, matching exactly. Unplotted milestones went 3 to 0, columns 39 to 31, horizon warnings 3 to 0. The old fixed window survives only as the fallback for an import with no usable dates at all. | FEAT-02, FEAT-07 | **H** | **Closed 2026-09-15** (v3.1.0-P28) | 2026-09-15 |
| TD-90 | Step 3 of setup: a From/To pair that overrides either end of the derived range, with the schedule's own earliest and latest dates stated above it so the min and max are readable while the range can still be set. Blank means the schedule's own end, so one field is a one-sided clamp rather than an all-or-nothing switch. The fields are deliberately NOT written back after a successful import: that path nulls `LAST_PARSE` and hides the whole section, so anything written there would be a value in a control nobody can see. The range that was used is reported by the Currently Imported Schedule panel, which is the surface that survives. | FEAT-02, FEAT-07 | M | **Closed 2026-09-15** (v3.1.0-P28) | 2026-09-15 |
| TD-91 | A user-defined date range filter in the top filter bar. It narrows the board, hiding the week columns outside it and shrinking the month bands' colspans to match, which is different from what the week dropdown does (marks a week, hides rows, leaves every column on screen, TD-79) and deliberately so: a range someone typed is asking for a narrower board. Both controls end as one `[lo,hi]` column pair, so there is a single definition of "in range" and a week selection cannot reach back outside an active range. Reapplied from `rerender()`, since `teardown()` rebuilds every cell and `<col>`. | FEAT-06 | M | **Closed 2026-09-15** (v3.1.0-P28) | 2026-09-15 |
| TD-92 | **Both fitters returned silently whenever a filter hid the first row.** `fitToScreen()` and `fitPrintPage()` measured `document.querySelector('tr.data')` to learn the non-week column width. A hidden row has no layout, so every cell in it reports `offsetParent === null`, the visible-cell list came back empty and both functions returned having done nothing. Reads as a dead button. Found by the P28 print-plus-range check, not by reading: the A3 preview refused to refit a range-narrowed board. Both now take the first row that is actually laid out. | FEAT-07 | M | **Closed 2026-09-15** (v3.1.0-P28) | 2026-09-15 |
| TD-93 | A range spanning more than `MAX_TIMELINE_WEEKS` is cut off with a diagnostic rather than built. One mis-parsed date far in the future would otherwise build a timeline of thousands of columns and hang the page. The guard is reported when it bites; it is not a preference and should not be raised to accommodate a genuinely long schedule without checking what made the range that wide. | FEAT-02 | L | **Closed 2026-09-15** (v3.1.0-P28) | 2026-09-15 |
| TD-94 | **The settings drawer rebuilt on a component set.** The panel carried 31 inline padding and margin declarations across 18 distinct values, four near-identical bordered-box classes (`.mnt-slot`, `.sd-src-box`, `.sd-src-text`, `.imp-fail`), five near-identical pill classes and three unnamed border radii. Replaced with the `sd-` primitives: `.sd-group`, `.sd-row` (with a `--split` label-column variant), `.sd-card` (with `--pick` and `--empty`), `.sd-badge` with five modifiers, `.sd-step`, `.sd-choice`, `.sd-icon-pick`, `.sd-stat` and `.sd-actions`, all resolving to six drawer-scoped spacing tokens declared once on `#settings-drawer`. Nine of the twelve primitives replace something that already existed in duplicate, so ten CSS rules were deleted outright. Gutter, row step, separator and radius are now asserted as single values rather than claimed. | FEAT-07, FEAT-14 | M | **Closed 2026-09-16** (v3.1.0-P29) | 2026-09-16 |
| TD-95 | The drawer becomes four tabs (Sources, Import, Defaults, Diagnostics) plus a sticky action footer, instead of one scroll of six sections with the terminal actions at the top. Settings that outlive an import (risk thresholds, week ending, fallback window) move to Defaults, out of two nested disclosures inside Import where the risk thresholds' own note already said they did not belong. `import-sect` and `diag-sect` keep their ids so every existing reference resolves; the two call sites that forced the old `<details>` open now call `setSettingsTab('import')`. The four numbered setup sections become a stepper that shows its state rather than appearing and disappearing, which is what `syncImportSteps()` reads back off the DOM. Tab choice is display state, persisted, and survives a rebuild. | FEAT-07 | M | **Closed 2026-09-16** (v3.1.0-P29) | 2026-09-16 |
| TD-96 | **`--color-text-note` measured 2.98:1 on the drawer background, under the 3.0 gate, and had measured that all along.** No probe had ever put that ink on `--color-bg-subtle`, so reading the CSS could not have found it and the gate reported zero. Exposed only because the rebuild made helper text a primary component rather than an occasional footnote. Darkened to `#767b85`: 3.90:1 on the panel, 4.25:1 on a card, still clearly lighter than `--color-text-muted` so the hierarchy holds. Same run also found `.ingest-status.ok` frozen: it hardcoded `#1a6b3a`, and the obvious replacement `--color-health-good` is a constant FILL, so using it as ink froze it again. Now `--color-ok-text`, a themed pair matching `--color-crit-text`. Both are the TD-28 shape: a fill token pressed into service as ink. | FEAT-14 | M | **Closed 2026-09-16** (v3.1.0-P29) | 2026-09-16 |
| TD-97 | `--color-purple-deep` is `#5a3a8e` in dark theme and is painted as ink on the dark panel background by `.cv-sect-title`, `.cv-cols-expander summary`, `#filter-bar .fb-hd` and `.fb-sect-title`, measuring 1.79:1. The token cannot simply be lightened for dark: it is also a fill at `tr.subtotal-row td.c-wk.now-col` and in `.hist-bar`, and ink on the light `--color-accent-tint-bg` strip at `tr.hdr-wk th.now-wk`, all three of which would break. The drawer's own occurrences were moved to a new `--color-accent-ink` at v3.1.0-P29; the four View Controls occurrences were left, because P29 was scoped to the drawer and silently restyling a second panel is not a design-system change anyone asked for. Fix by pointing them at `--color-accent-ink` too, with probes added for each. | FEAT-14 | M | Open | 2026-09-16 |
| TD-98 | TD-88's duplicate `--color-bg-subtle` is load-bearing, which the consumer sweep it was waiting on now shows. Removing it makes the token `#232f4a` in dark, and `tr.hdr-wk th` paints `--color-text-small` on it, which is `#334` in dark: an ink that only works on a light strip. So the week header is deliberately a light strip in dark theme and the duplicate is what keeps it that way. The drawer was moved to `--color-bg-panel` at v3.1.0-P29 rather than removing the duplicate, so it is correct in both themes and TD-88 is untouched. Closing TD-88 needs the week header and legend given their own explicit light-strip token, not the duplicate deleted. | FEAT-14 | M | Open | 2026-09-16 |
| TD-99 | **`source` on the data model.** Every milestone gains `source` and every row `sourceSchedule`, stamped by `aggregate()` from the name set at step 4 of setup. Deliberately not named `src`: `task.src` already exists and holds the comma list of Activity IDs behind a row, which is why the board's Source column is renamed **Source Activity IDs** and the new one is **Source Schedule**. The seeds predate the field and are backfilled at load rather than rewritten across 357 literals, so the embedded baseline filters as a real source instead of as a blank. | FEAT-02, FEAT-06 | M | **Closed 2026-09-16** (v3.1.0-P30) | 2026-09-16 |
| TD-100 | **The source registry.** `PRIMARY_SOURCES[]` holds one entry per mounted schedule and `UPDATE_TASKS`/`UPDATE_MILESTONES` become its concatenation, rebuilt in one place by `rebuildPrimaryDatasets()`. This matters because `setViewMode()` re-slices `TASKS` and `MILESTONES` from the `UPDATE_*` pair on every view switch: anything pushed straight into `TASKS` is gone the moment the user toggles to baseline and back. The mount panel becomes one card per source with Rename and Remove, and removing the last source is still `discardUpdate()` so the reset to baseline keeps its confirm. | FEAT-02, FEAT-07 | M | **Closed 2026-09-16** (v3.1.0-P30) | 2026-09-16 |
| TD-101 | **Append, with duplicate Activity IDs given a numbered suffix.** A second schedule runs the normal pipeline and is concatenated. Band order survives because the P22 `bandIdx`/`rowIdx` sort runs per `aggregate()` call, so the appended bands follow the first schedule's rather than interleaving. `dedupeIncomingIds()` rewrites `m.id`, `m.notes`, `m.ref`, `t.ref` and `t.src` in **one** pass from one rename decision: `MS_SHORT_TITLES`, `MS_COMMENTS` and `MS_HEALTH_OVERRIDE` all key through `msKeyFor()`, `getMilestones()` keys rows on `task.ref`, and `task.ref` is itself an Activity ID, so rewriting any one of those without the others silently merges two schedules' annotations. Verified by appending the reference workbook to itself, where every single ID collides. | FEAT-02 | **H** | **Closed 2026-09-16** (v3.1.0-P30) | 2026-09-16 |
| TD-102 | **`m.id` is the source of truth for an Activity ID.** Four readers re-derived it by running a regex over `m.notes`: `msKeyFor()`, `renderRows()`, `allMilestoneIds()` and `findMilestoneBySnip()`. That cost a regex per milestone per call on paths that run per keystroke, and it made `notes` the de facto key, which is exactly what makes a rename a three-field operation. One `msId()` helper reads `id` and falls back to the regex only for records that predate it. The regex character class also gains `_` so a suffixed `SNIP-101_1` round-trips. | FEAT-02, FEAT-16 | M | **Closed 2026-09-16** (v3.1.0-P30) | 2026-09-16 |
| TD-103 | **Four quadratic paths removed before append could double the dataset.** `getMilestones()` filtered all of `MILESTONES` once per row inside `renderRows()`, i.e. O(rows x milestones); it now reads a per-rebuild `ref` index. `allMilestoneIds()` de-duplicated with `indexOf` inside its own loop and is called on every keystroke in the Activity ID field; it uses a Set and is cached. `findMilestoneBySnip()` scanned with a regex per element per dependency endpoint; it reads a built map. `applyDateRangeColumns()` wrote an inline style to every week cell in every row on every filter pass; it generates one stylesheet rule per hidden column. The two `oninput` filters are debounced at 60ms. All four are asserted by counting, not timing: `performance.now()` does not advance under Chromium's virtual time, and the first draft of the check reported 0ms against every budget. | FEAT-16 | M | **Closed 2026-09-16** (v3.1.0-P30) | 2026-09-16 |
| TD-104 | The setup stepper's fixed-column count is load-bearing in two places that look nothing like a column definition: `teardown()` and the publish scrubber both strip week header cells with `#phase-hdr th:nth-child(n+N)`. Adding the Source Schedule column moved N from 10 to 11. Replaced with `FIXED_COL_COUNT` and one derived selector, because the literal was invisible from anywhere a column is actually declared and a future column would break the month band silently. | FEAT-07 | L | **Closed 2026-09-16** (v3.1.0-P30) | 2026-09-16 |
| TD-105 | **Dependency lines vanished whenever a filter was active, which reads as the feature being broken.** `markerCenter()` guarded only `if(!el) return null`. A marker whose ROW is filtered out, or whose WEEK COLUMN is hidden by the date range, stays in the DOM and still answers `querySelector`; it simply has no layout, so its rect is all zeros. Measuring that produced a point at the board's top-left corner rather than "no point", so every line to a filtered marker was drawn to the same invisible spot. Measured at 308 of 331 paths collapsed to zero size under a week plus date-range filter, and 0 visible. Same family as TD-92: an element that exists but has no layout. `markerCenter()` now returns null for a zero rect, which routes the target into the existing "not on board" stub that already exists for exactly this case. | FEAT-04, FEAT-06 | **H** | **Closed 2026-09-16** (v3.1.0-P31) | 2026-09-16 |
| TD-106 | Dependency lines are drawn once into fixed content coordinates, so they do not follow a filter on their own: filtering left them frozen wherever the markers used to be. `applyFilter()` gained the redraw at both of its exits. That was not enough: `clearFilter()` un-hides every row and column **itself** rather than routing through `applyFilter()`, so it is a second entry point into "what is on the board just changed" and Remove all filters restored the rows while the lines stayed suppressed, 0 of 477 coming back. Found by the regression check written for TD-105, not by reading. Third instance of the standing trap: when a pipeline gains a stage, audit every entry point that should trigger it. | FEAT-04, FEAT-06 | M | **Closed 2026-09-16** (v3.1.0-P31) | 2026-09-16 |
| TD-107 | Reported alongside TD-105, **reproduced and closed as TD-108/TD-110** once the user supplied their published board. The original note read: milestone icons sitting outside the row bounds when several share a cell. **Not reproduced at the time.** The reference workbook's densest cell holds two markers and measures clean at every slider setting tried (0 of 8 outside the row, worst overhang 4.3px INSIDE the boundary at 10px icons on a 59px column). The reported board is a different export, 109 tasks / 150 milestones against the reference's 105 / 146, and shows **three** markers in one cell. TD-78 already records that three 15px icons cannot fit a 36px column without overlapping, and the standing trap is explicit that this system was built as a two-state toggle and re-collided at N=3. The export arrived and the real defect turned out to be the LABEL stack, not the icon, plus a second icon defect at large icon sizes. | FEAT-05 | M | **Closed 2026-09-16** (v3.1.0-P31) | 2026-09-16 |
| TD-108 | **Label stacks landed on the neighbouring row whenever two markers collided.** Two offsets compounded: `msCellPos()` moves a shared marker off the cell midline by a fixed PERCENTAGE, and the label band then moved the stack again from there. A stack in ID + Title mode is 25px; a 34px row leaves 4.5px of slack each side; the band was a fixed 15px. So a banded label necessarily sat ~10.5px outside its row. Measured on the user's own published board: 31 of 150 stacks overflowing, worst 18.2px. TD-76 fixed exactly this for the ICON at P25 and the comment in the CSS says so; the label half kept the fixed offset and the defect survived there. Fixed by pinning the stack to the CELL midline (one `--lbl-band-dy` carrying the whole offset, set per marker where both the spread and the row height are known) and by holding rows at 48px while labels are shown, which is what gives the three bands room. 0 outside at every slider setting tried. | FEAT-05 | **H** | **Closed 2026-09-16** (v3.1.0-P31) | 2026-09-16 |
| TD-109 | Row height is now a Layout control, and the Layout section is ordered Title col, Icon size, Week col, Row height, wrap toggle. The slider is a floor rather than a ceiling: while milestone labels are shown, rows are held at the 48px the bands need and the readout says so, and asking for more than that is always honoured. | FEAT-07 | M | **Closed 2026-09-16** (v3.1.0-P31) | 2026-09-16 |
| TD-110 | **The icon size slider never rebuilt the board, so marker placement stayed computed for the previous icon size.** `setIcoSize()` resized the SVGs in place and `onSizeSliderRelease()` only redrew dependency lines. Marker placement depends on icon size (the clamp keeping an icon inside its row lives in `renderRows()`), so at 24px on a 34px row 10 markers sat outside the row and no render-time clamp could help, because render never re-ran. Found while fixing TD-108: the first clamp measured identical numbers before and after, which is what a stale render looks like. Both sliders now flag a rebuild and `onSizeSliderRelease()` does one, once, on release rather than per drag tick. | FEAT-05, FEAT-07 | M | **Closed 2026-09-16** (v3.1.0-P31) | 2026-09-16 |
| TD-111 | **Marker placement rebuilt on pixel offsets from the cell's true centre.** `msCellPos()` returned percentages of a height the render cannot know, so both corrections layered on it (the icon clamp and the label `undoSpread`) were computed against an assumed `effectiveRowHeight()`. It also meant the spread was a FRACTION of the row: 22% is 7.5px at 34px and 14px at 65px, so raising row height scattered the markers instead of settling them, which is what the user was looking at. `.m-wrap` now anchors at `left:50%; top:50%` (the cell's real padding box, whatever the row turns out to be) and `msCellOffset()` returns `--mdx`/`--mdy` in pixels, budgeted from `ROW_HEIGHT` and `COL_WIDTH`. Those are minimums the rendered cell can only exceed, so the error direction flips to "used less room than was available", which is invisible, from "used more", which was the report. The clamp is gone: the budget makes the offsets fit by construction. | FEAT-05 | **H** | **Closed 2026-09-17** (v3.1.0-P32) | 2026-09-17 |
| TD-112 | **The shared-cell spread was a two-state toggle and had been since P25.** `msCellPos()` set `y = 50 ± 22` on `i%2`, so measured it produced exactly two distinct y values for every N from 2 to 6: N=3 came out `[28, 72, 28]` with the first and third marker on top of each other, and from N=5 their icons overlapped for real (0.6px at N=5, 3.5px at N=6, against a 15px icon). This is verbatim the standing trap in `CLAUDE.md`: the LABEL collision system was rebuilt as three bands at P25 for exactly this reason and the ICON half was left a toggle. Replaced with a diagonal cascade, `slot = i - (n-1)/2` applied to both axes, so the offsets are monotonic across and down and no two markers in a cell can share a position at any N. Asserted against `msCellOffset()` directly, because the reference board's densest cell holds two markers and cannot reach the defect. | FEAT-05 | **H** | **Closed 2026-09-17** (v3.1.0-P32) | 2026-09-17 |
| TD-113 | **Milestone labels no longer change the row height, reversing TD-109.** The 48px floor was rejected by the user: "should be able to toggle the labels on and off without impacting the layout. If the rows are too condensed, there is the row height control." It was also broken in its own terms: `toggleLabels()` called `applyRowHeight()` and then `onSizeSliderRelease()`, which only rebuilds when a flag is set and neither path set one, so measured, turning labels on moved 105 of 105 rows from 34.34px to 48px while 0 of 146 markers were repositioned. `setShortTitleMode()` did the same state change correctly, two entry points wired differently. `LABEL_ROW_MIN`, `labelsNeedTallRows()` and `effectiveRowHeight()` are deleted and the band is a fixed 13px. Accepted trade, stated rather than hidden: on a short row a banded label overlaps the gutter instead of the row growing under it. Bounded at one label line, and measured to reach zero spill by 65px, so the row height control is a real remedy rather than advice. | FEAT-05, FEAT-07 | **H** | **Closed 2026-09-17** (v3.1.0-P32) | 2026-09-17 |
| TD-114 | **Hiding the filter row hid the only control that could bring it back.** `#btn-filter-icon` lived inside `#top-filter-bar`, which collapses to `max-height:0` with `overflow:hidden`, so one click removed the filter row for the rest of the session. Same family as TD-92: an element still in the DOM, still answering `querySelector`, with no layout at all. The toggle moves to the header icon bar as `#btn-filter-toggle`, outside what it hides, carrying the active-filters dot and reflecting its own state through `aria-pressed` and `.on`. The bar keeps a right-hand `#btn-filter-hide`, which is only safe because the header control now outlives it. | FEAT-06, FEAT-07 | **H** | **Closed 2026-09-17** (v3.1.0-P32) | 2026-09-17 |
| TD-115 | Two fields drove one title filter. P23 moved the corner search inline with the other filters and left the old Title contains group in place; both wrote the same value through `syncTitleFilterInputs()`, so they were duplicates in the literal sense. The duplicate group is removed and the surviving field takes the id `filter-title`, which `applyFilter()` and `clearFilter()` already read, so no call site moved. | FEAT-06 | M | **Closed 2026-09-17** (v3.1.0-P32) | 2026-09-17 |
| TD-116 | **Every rebuild silently cleared the week filter.** Reported as "changing a health icon unfilters the dashboard", and health was only one of many callers: `teardown()` removes the week options and the selection goes with them, and `rebuildWeekFilter()` never put it back, so any `scheduleRerender(true)` dropped it. `rebuildBandingFilter()` and `rebuildSourceFilter()` both already saved and restored their selection; this one did not, which is the standing "audit every entry point" trap again, a rule applied in two of the three places that need it. It read as the board unfiltering itself because the action that lost the filter had nothing to do with filtering. The selection is now remembered across the rebuild, and restored only if it is still a valid index, tested at both bounds since a rebuild can shorten the timeline. | FEAT-06 | **H** | **Closed 2026-09-17** (v3.1.0-P32) | 2026-09-17 |
| TD-117 | The Row height slider's bottom of range is dead: asking for 28 produces a 34.34px row, because the text in the fixed columns sets the floor, not `--row-min-h`. Found while measuring TD-111, and deliberately not "fixed" by raising the slider minimum, because the floor moves with the Title col width and the label scale, so a fixed minimum would be wrong at other settings. `td.c-wk{min-height:38px}` was removed in passing: it read as a 38px floor but Chromium does not honour `min-height` on a table cell, so it was dead and would have been a second, invisible authority fighting the control in any engine that does. Placement is unaffected either way, since offsets are budgeted from the asked-for height and anchored at the cell's real centre. | FEAT-07 | L | Open | 2026-09-17 |
| TD-118 | Two assertions in the first TEST-34 run were wrong about the app rather than the app being wrong: the right-alignment check read `getComputedStyle().marginLeft` expecting the literal `auto` (it reports the used pixel value, 911px), and the baseline-ghost block measured an empty set because the overlay is off by default. The sample-size rule caught the second, as designed. The first is a new shape worth naming: asserting on a CSS input value where only the computed output is observable. | FEAT-16 | L | **Closed 2026-09-17** (v3.1.0-P32) | 2026-09-17 |
| TD-119 | **Marker placement is staggered at the MARKER, not the label.** P32 kept every icon on the row's centreline and moved only the text, so a label's vertical position said nothing about which marker owned it. A row's markers are now cut into proximity runs (more than `MS_PROXIMITY_COLS` from the previous one starts a new run; a run of one is dead centre) and each run's members are put on bands: top/bottom for two, top/middle/bottom for three, and a triangle wave beyond, so the fourth is the middle band and the fifth returns to top. A plain `[0,1,2]` repeat would put markers one and four on the same line three columns apart, which is the two-state trap already recorded at TD-108 and TD-112. `--lbl-dy` and the `.lbl-band-*` classes are gone: the label is a child of the wrap and rides it. Measured against the P32 release with the same sweep, labels outside their row fall from 140 to 62 across seven settings and no setting spills deeper. | FEAT-05 | **H** | **Closed 2026-09-18** (v3.1.0-P33) | 2026-09-18 |
| TD-120 | `msCellOffset()` is horizontal-only. Markers sharing a cell are consecutive within their proximity run, so the band has already put them on different lines; a second vertical offset here would compound with it, which is exactly the failure P32 removed between the icon spread and the label band. A cell holding more than three markers is the one case the bands cannot serve, because those markers share an x: that row, and only that row, grows via `--row-h-eff` by the minimum that gives each of them a line. Data-driven rather than display-driven, so a denser import can change row heights and no toggle ever does. The reference board's densest cell holds two, so the growth path is asserted by cloning markers into a cell, never by the data. | FEAT-05 | M | **Closed 2026-09-18** (v3.1.0-P33) | 2026-09-18 |
| TD-121 | **One `renderMarker()` factory builds every `.m-wrap`.** There were three hand-written builders (live marker, baseline ghost, LoE pill), the ghost only a partial copy of the live one, kept in step by memory. That is not a tidiness point: the ghost's rule is X from its own column and Y from its pair, so it must carry the same vertical offset as the marker it belongs to, and TD-119 changes how that offset is computed. With two writers it would have desynced silently. The factory is now the only thing that writes `--mdx`/`--mdy`, asserted by grep and by a live-DOM audit. Scope is the wrap only; callers append their own children and keep their own click and drag wiring. | FEAT-05, FEAT-16 | M | **Closed 2026-09-18** (v3.1.0-P33) | 2026-09-18 |
| TD-122 | `markerCenter()` discovered that a marker was filtered out by measuring a zero rect, once per dependency endpoint inside `drawDepLines()`'s loop, which forces layout every time. It now asks `markerHidden()` first, which reads the row's `hidden-row` class and the cell's `data-col` against `DATE_RANGE_COLS`: no layout, and no new state. Deliberately **derived, not stored**. A flag would need writing at `applyFilter()`, `clearFilter()`, `applyDateRangeColumns()` and the rebuild, and a stale one would make dependency lines vanish exactly as in TD-105 but harder to find, because measurement would no longer be the truth. The zero-rect test stays as a backstop and the check asserts the two agree on every marker under four filter combinations. | FEAT-04, FEAT-16 | M | **Closed 2026-09-18** (v3.1.0-P33) | 2026-09-18 |
| TD-123 | **Three probes were driving placement through the setters alone and measuring nothing.** `p30_check`, `p32_check` and the screenshot probe called `setIcoSize()` / `setWkWidth()` directly, but `rerender()` reapplies display settings by reading the slider ELEMENTS back, so each call was undone by the rebuild it triggered and every sweep measured the default over and over. Found by a diagnostic that asked for a 10px icon and measured 15, while investigating what looked like 17 markers leaving their row; with the settings actually applied, that failure did not exist. Fifth vacuous-pass family in this project (TD-66, TD-87, the P26 float column, the P29 drawer probes). All three now set the control and the variable. | FEAT-16 | **H** | **Closed 2026-09-18** (v3.1.0-P33) | 2026-09-18 |
| TD-124 | The label stack anchors at `left:50%` so it unfurls from behind its icon, with the chip's text indented `calc(4px + var(--ico-half))` so the icon covers no glyph. That made `body.counts-on`'s margin short by half an icon, so with dependency counts on the stack sat under the right-hand count chip, which is the TD-83 defect returning. Caught by `p27_check`, not by reading. The counts-on rule now clears the icon's half as well and drops the text indent, since with counts on there is no unfurl to preserve. | FEAT-05 | M | **Closed 2026-09-18** (v3.1.0-P33) | 2026-09-18 |
| TD-125 | Label spill is no longer monotonic in row height, and the `p32_check` assertion that said it was has been inverted rather than deleted. The band amplitude now scales with the row until it caps, so going from a 28px row to a 34px row buys a bigger stagger before the row is tall enough to contain it: spill rises, then clears entirely by 48px. The promise the Row height control actually makes, that raising it clears the spill, still holds; "every increase helps" does not. Worth revisiting if a user reads 28 to 34 as a regression. | FEAT-05, FEAT-07 | L | Open | 2026-09-18 |
| TD-126 | **The level cycle resets every third marker, reversing TD-119's triangle wave.** Row 69 of the Aug-29 board carries four markers in directly adjacent columns (18, 19, 20, 21) and the fourth, SNIP-212, sat on the middle band. The user asked for it to reset to the top. Capping the run length cannot do that: a run of one is dead centre by definition, so splitting the run at three would have left SNIP-212 in the middle again, which was checked before choosing. `MS_LEVEL_CYCLE` becomes `[0,1,2]` and the fourth marker returns to top. The cost TD-119 predicted is real and accepted rather than hidden: markers one and four now share a line three columns apart. The comment arguing against the repeat is kept and rewritten to record that the reversal came from a rendered case, so the wave is not restored from the old reasoning alone. | FEAT-05 | **H** | **Closed 2026-09-18** (v3.1.0-P34) | 2026-09-18 |
| TD-127 | **The proximity boundary is two blank columns, and is deliberately not a label width.** `MS_PROXIMITY_COLS` was four, justified in the file as roughly how wide a rendered label is. That made marker anchoring a function of the label scale controls, so changing the text size would re-anchor the board. The user's words choosing the replacement: "if you change the size of the text, this would change the layout, which we don't want." A run now continues while consecutive markers are at most two blank columns apart, a gap of three, and the threshold is a fixed count of schedule columns. Measured effect: it splits three runs the old window held together. Also fixed here, and separately: two markers sharing one cell were excluded from the symmetric pair rule and took top and middle rather than top and bottom, `msRunLevels([0,1],3)` giving `[0,2]` while `msRunLevels([5,5],3)` gave `[0,1]`. No row on this board reaches it, so it corrects an inconsistency rather than a visible defect, and the check says so. | FEAT-05 | M | **Closed 2026-09-18** (v3.1.0-P34) | 2026-09-18 |
| TD-128 | **Two header heights CSS was guessing at are measured now.** The week header row stuck at the literal `19.5px`, a stand-in for the month row's height, which renders 16px: a 3.5px band between the two sticky rows that data rows scrolled through, present at 390, 768 and 1440 wide, so never a compact-mode fault, only easiest to see on a phone. The filter bar's open cap was the literal `160px`; the bar wraps, so at 390px its content came to 202px against a 159px box and the part cut off read as the panel hiding beneath the header below it, while at 1440px the content is 63px, which is why it never showed on a desktop. Both come from one writer, `watchStickyHeights()`, as a `ResizeObserver` each on `#phase-hdr` and `#top-filter-bar`, rather than hooks in `rerender()`, `applyRowHeight()`, `togglePrintMode()`, `fitToScreen()` and a resize listener: five entry points, and a missed entry point has cost this project a cycle four times. Two things measured rather than assumed: the month height is read from the ROW, since the metadata cells carry `padding:0` and the month cells 3px, and the bar's cap is a deliberate over-estimate, because while it is closed its vertical padding has transitioned away and an exact figure taken then would clip on the way back open. `--hdr-search-h` and the dead `tr.hdr-search` rule go with it; the token was declared `0px` and never written. | FEAT-05, FEAT-07 | **H** | **Closed 2026-09-18** (v3.1.0-P34) | 2026-09-18 |
| TD-129 | **A reported defect that measurement refuted, recorded so it is not re-fixed from the report.** SNIP-218 and SNIP-219 were marked up as "not anchored properly, SNIP 219 needs to move down slightly, they should be an equal distance from the top and bottom margins". They are in different rows (83 and 14), each the only marker in its row, and both measure dead centre of their own row at 390 and 1600 wide. The pair rule the report seemed to point at was not what was rendering. The likely cause is TD-128's sticky header seam: two single-marker rows straddling a 3.5px band that data scrolls through do not read as evenly set. Left open pending the user's own look at the P34 build, rather than closed on a guess. | FEAT-05 | L | Open | 2026-09-18 |
| TD-130 | **The milestone card is 10% smaller and its schedule row is one row.** Width cap 340px to 306px (exactly 0.9x), padding, vertical rhythm and the two largest type steps down with it; measured against the P34 release in the same browser at the same viewport on the same milestone, the card is 10.0% narrower and 22.6% shorter. Start, Finish and Progress now share one row, with a full-height column divider between Finish and Progress and all three values at one computed text size: Start was 14px against Finish at 18px, which read as two different kinds of fact rather than one row. MS weight, MS hours and earned hours move into a `<details>` that is collapsed on every card open, not left wherever the last one was put. | FEAT-05, FEAT-07 | M | **Closed 2026-09-18** (v3.1.0-P35) | 2026-09-17 |
| TD-131 | **Progress is editable, and is annotation-layer state.** `MS_PROGRESS_OVERRIDE` keys through `msKeyFor()` alongside the three existing stores, has its own `ANNOT_CATEGORIES` entry, round-trips through publish and the model export, and moves with a milestone dragged to another row. `effectiveProgress(m)` is the only reader and every consumer goes through it: the card, the bar, earned hours, the milestone tooltip and `computeProgress()`, which is the row's rollup, so a card cannot disagree with the row it sits on. The milestone record is never written, which is what makes clearing the field restore the schedule's own value. The field carries the `.changed` indicator the row remarks field uses, and commits on blur or Enter rather than per keystroke, because a commit rerenders the board. **An override equal to the schedule's own value is discarded rather than stored**, which is the opposite of `MS_HEALTH_OVERRIDE` and is recorded as a decision: a health value of "explicitly N/A" is distinct from no opinion, a progress of 40 against a schedule that says 40 is not, and storing it would inflate the annotation count a reader is shown and make the edited indicator lie. Blank and unparseable both restore; over 100 clamps to 100 and under 0 to 0. Marking the icon complete sets 100%, one direction only: clearing the Complete dot does not drop it back, because by then the user may have typed over it. | FEAT-05, FEAT-07 | **H** | **Closed 2026-09-18** (v3.1.0-P35) | 2026-09-17 |
| TD-132 | **The card was writing annotations under a key it composed itself.** `openMsDialog()` built `snipId||('noid-'+...)` from `extractSnipId(m.notes)` while every reader uses `msKeyFor()`, which prefers `m.id`. Measured across the reference board: 146 milestones, **0 divergent**, so this is hygiene on this dataset rather than a live defect, and the check reports the count rather than asserting the two can never differ. Nothing enforced the equality, so a milestone whose `id` and `notes` disagreed would have had its comment, health override, short title and now its progress written where the board never read them. The card now sets `msDialogFor=msKeyFor(m)` and holds the milestone and its row hours for the duration, which also removes the `findMilestoneBySnip()` lookup that returns null for any milestone with no ID. | FEAT-05 | M | **Closed 2026-09-18** (v3.1.0-P35) | 2026-09-18 |
| TD-133 | **Three probe defects and one vacuous comparison, found by the P35 run.** (1) A child of a CLOSED `<details>` reports a non-zero `getBoundingClientRect()` in this headless build, verified against a three-line page for both a plain div and a grid, so the fold assertion was measuring nothing; it uses `checkVisibility()` plus the fold's own height now. Sixth headless measurement artefact here. (2) The tooltip assertion read `data-tip` off the wrap handle captured before the edit, which a commit detaches, so it measured the old board. (3) The clamp assertion read the store after typing `-5`; the target's schedule value is 0 and `-5` clamps to 0, so it was testing the deduplication rule rather than the clamp, and now asserts the effective value. (4) The Start/Finish/Progress row was asserted on a milestone whose Start field is `display:none`, so it compared two boxes while claiming three. Second time a milestone-card assertion has compared against a field that was not rendering (TEST-29). | FEAT-05 | M | **Closed 2026-09-18** (v3.1.0-P35) | 2026-09-18 |
| TD-134 | **Seven header icon buttons became one More Actions menu.** `.ib-right-icons` carried a fixed 254px of buttons (7 x 26 + 6 x 12) hard against the right edge, and `#ib-label` is the flex item that gives way. Measured on the previous release, the label needs 215.8px and got **35.2px at 390 wide and 162px at 768**, so the app name and version it carries were unreadable on anything but a wide desktop. One 26px trigger returns 228px of that. Every row keeps the id its button carried, so `toggleSettingsDrawer`, `toggleFilterBar`, `toggleTopFilterBar`, `togglePrintMode` and the two dot writers were not touched at all, which is asserted by diffing those function bodies against the previous release rather than claimed. Two things did need care: `.icon-btn.on` and `.icon-btn.ib-mi` have equal specificity, so the active state would have been lost to source order and the rule is restated (asserted on computed background, not class presence); and `toggleTheme` rewrote the button's whole `innerHTML`, which would have eaten the row's label, so one `setThemeGlyph()` writer now targets the icon span and both call sites use it. | FEAT-07 | M | **Closed 2026-09-19** (v3.1.0-P36) | 2026-09-18 |
| TD-135 | **At 390px wide the version label is still clipped, and this is a stated limit rather than a defect.** The consolidation takes it from 35.2px to 167px of the 216px it needs, a 4.7x gain, but the bar at phone width also carries the editable report title, so 216px of label plus that title plus the trigger does not fit in 390px. Unclipped at 768 and 1440. The check asserts this conditionally on measured room rather than on a viewport width typed into it, since a hardcoded breakpoint would be a constant standing in for a measurement, which is a recurring defect family here. Resolving it means deciding what gives way at phone width, the title or the label, and that is a design call nobody has made. | FEAT-07 | L | Open | 2026-09-19 |
| TD-136 | **`checkVisibility()` does not answer "is this hidden" in general, and the TD-72 guard was weaker than it looked.** Moving the filter-row toggle into the menu broke the `p32_check` assertion that it stays on screen when the filter row is hidden, which is the TD-72 rule. The rule still holds (the trigger outlives the bar and exposes the toggle), so the assertion was rewritten to follow the path. A negative control was then added, putting the toggle back inside the collapsed bar and requiring the same expression to go false, and **the first rewrite passed it**, so the measurement was wrong. Measured against this build, a control trapped inside a `max-height:0; overflow:hidden` bar reports `offsetParent` non-null, height 24px, one client rect and `checkVisibility()` **true**: the clip does not zero its children's boxes. `elementFromPoint` did not discriminate either. What does is intersecting the element's box with every clipping ancestor, which leaves the trapped control at 0px. The guard now uses that and demonstrably fails when the defect is reintroduced. Note the contrast with the P35 finding: `checkVisibility()` is correct for a closed `<details>` and wrong here, so it is container-specific, not a general answer. | FEAT-07 | **H** | **Closed 2026-09-19** (v3.1.0-P36) | 2026-09-19 |
| TD-137 | **Two popups were clipped by an ancestor's overflow, and making them `position:fixed` was only half the fix.** The More Actions panel measured 184px tall with **0px visible**, clipped by `#icon-bar`'s `overflow-x:auto` (which computes `overflow-y` to auto with it); the ID suggestion list measured 200px with **36px visible**, clipped by `#top-filter-bar`'s `max-height:0/overflow:hidden`. Neither overflow can be removed: one lets the bar scroll at phone width, the other is the collapse mechanism. Both popups are fixed now and placed by one `positionFixedPopup()`. Freed of the clip the menu was then **painted over** by `#rpt-hd` and `#top-filter-bar`: `#icon-bar` is `position:relative` with a z-index and so a stacking context, and no z-index inside it can lift a descendant above a later sibling of the bar. Measured 0 of 7 rows hit-testable at 390 wide. The bar went z-index 30 to 40, which clears those two and the table headers (max 26) and stays below the drawers (700/850/900). Now 7 of 7 menu rows and 30 of 30 suggestions individually reachable at 390, 1440 and 2000. | FEAT-07 | **H** | **Closed 2026-09-21** (v3.1.0-P37) | 2026-09-21 |
| TD-138 | **The Title col slider read 220px while the column rendered 380.1px.** `#col-name` carries no width until `setNameWidth()` runs, and `setNameWidth` was in neither build path, so `table-layout:fixed` handed the title column whatever the collapsed fixed columns left over. Dragging up from the default therefore SHRANK the column to ~240 before it grew, which is why the control read as doing nothing. **The first measurement refuted the report** (at 1600 wide after an import the slider moved the column 295.3 to 380 and it survived a rerender); the report was right and the measurement was too narrow, missing the board as it actually opens: the seeded baseline with fixed columns collapsed. Fixed by `reapplyDisplaySettings()`, one function holding all five slider-owned settings, called from `rerender()` AND from init's first paint. The init block already carried a comment about that very trap while omitting three of the five. | FEAT-07 | **H** | **Closed 2026-09-21** (v3.1.0-P37) | 2026-09-21 |
| TD-139 | **The zero-dependency filter did nothing from the default state.** Its tooltip promised "Show only milestones with zero predecessors/dependencies" but it gated dependency-LINE drawing, and there are no lines until dependencies are switched on: measured 105 rows before and 105 after, with 0 lines drawn. It now narrows the board, living in `applyFilter()` so it composes with every other filter and is reapplied on rebuild like the rest. Measured on the baseline board: 159 rows to **46** for Only zero and **113** for Hide zero, the two complementary, 50 of 193 milestones carrying no dependencies, and the filter line states what it narrowed to. Also recorded: `drawUnresolvedStub()` emits the same `zero-stub` class as a true zero stub while meaning "N not on board", so the two marks are indistinguishable to CSS and to any probe. Left alone here, since nothing reported it. | FEAT-05, FEAT-07 | **H** | **Closed 2026-09-21** (v3.1.0-P37) | 2026-09-21 |
| TD-140 | **The clipping-aware measurement written at P36 cannot answer the question for a `position:fixed` element, and said the fix had failed.** It walks the DOM ancestor chain intersecting overflow boxes; a fixed element is not clipped by ancestor overflow at all, so it reported the fixed panel as 0px visible exactly as it had reported the absolute one. `elementFromPoint` at a single centre point was no better, returning false even for a panel that was genuinely on top. What answers it is a hit-test PROFILE: ask the document what paints at several points down the popup, and count how many of its children are individually reachable. **Third consecutive partial in which the measurement technique, not the code, was the thing that was wrong.** | FEAT-07 | M | **Closed 2026-09-21** (v3.1.0-P37) | 2026-09-21 |
| TD-141 | `drawUnresolvedStub()` marks its path with the same `zero-stub` class a true zero stub uses, while meaning "N not on board" rather than "no dependencies". Any CSS rule or probe targeting zero stubs catches both. Found while measuring TD-139 and left alone, because nothing reported it and changing a class the dependency layer keys on is its own change with its own verification. | FEAT-05 | L | Open | 2026-09-21 |
| TD-142 | **The print preview's two heading bars were viewport width while the sheet was A3.** `#icon-bar` and `.pm-banner` sit outside `#page-frame`, so they took the body's width, which is the viewport's. Measured on P37: both rendered **390px** wide at phone width and **1440px** at desktop against a **1122.5px** sheet, so they fell 732px short of it in one direction and overhung it by 317px in the other. Both now take the sheet's width, centring and side inset (`--page-margin` plus the frame's 1px border), so the outer edges AND the content boxes coincide with the report heading at every viewport. Second half: the preview gained three controls of its own in the banner (re-fit to page, leave preview, More Actions). Placing them at the right of a sheet-width strip put **0 of 3 on screen at 390 and 2 of 3 at 1024**, because an A3 sheet is wider than either viewport and the page scrolls sideways; they sit at the sheet's left edge instead, which is on screen at every width. | FEAT-07, FEAT-13 | **H** | **Closed 2026-09-22** (v3.1.0-P38) | 2026-09-22 |
| TD-143 | **The date range filter worked and could not be reached.** The report was that setting one end did nothing. The first measurement **refuted it as written**: with a `change` event dispatched, an end date alone narrowed the board to 20 of 39 weeks and a start date alone to 24 of 39, at both 390 and 1440, because `dateRangeToCols()` has always left the other end open. What did not work was the DELIVERY: `change` on `<input type="date">` does not fire until the field is committed and left, so a date set with the picker or the spinner sat there until focus moved elsewhere. Both fields now also fire `oninput`, debounced through the same `scheduleFilter()` the title field uses. The assertion dispatches `input` **only** — a probe that fires `change` cannot see this defect at all, which is exactly why the earlier ones passed. | FEAT-06 | **H** | **Closed 2026-09-22** (v3.1.0-P38) | 2026-09-22 |
| TD-144 | Default view changed on the user's request: milestone hours labels **off**, Activity ID labels **on** (`SHORT_TITLE_MODE='id'` with `shortTitlesVisible=true`). Each default is stated twice, once in the script and once as the markup's `active`/`checked` state, and both are read at first paint, so the source check asserts the pair agrees. 38 of the 196 milestones on the seeded board carry no Activity ID and correctly fall back to their title, so "every label is an ID" is the wrong assertion; what distinguishes `id` from `both` is that no label carries the ` _ ` joiner, asserted with a negative control that switches to `both` and requires the same 158 to gain it. | FEAT-05 | M | **Closed 2026-09-22** (v3.1.0-P38) | 2026-09-22 |
| TD-145 | **`let mHrsVisible=false` rendered 196 visible hours labels at first paint.** The rebuild path hid all three things drawn on the board that a fresh render creates visible (type-code label, milestone hours, remarks) and init's first-paint path hid only the label, so the new default did not take effect until something happened to trigger a rebuild. Measured 196 of 196 shown with the checkbox reporting false. **Third occurrence of this family** (TD-59, TD-71, TD-139 in kind, TD-138 immediately before it), so it was folded into `applyMarkerLabelState()` inside the one `reapplyDisplaySettings()` writer both paths already call, and the three lines were deleted from both call sites rather than a fourth line added to one. Written both directions rather than hide-only, so the function states the state instead of depending on what a fresh render leaves behind. | FEAT-05, FEAT-07 | **H** | **Closed 2026-09-22** (v3.1.0-P38) | 2026-09-22 |
| TD-146 | **A size slider that scales text which is switched off is a control with no effect, and it read as broken.** Each of the three moved directly under the control that turns its text on, and is disabled while that text is off. The enabled state is derived by one writer (`syncLabelScaleEnabled`) from the same three variables the board reads, called from every path that changes them including the rebuild reapply, rather than toggled from each handler. `disabled` is the real barrier and it is **not observable**: `dispatchEvent` reaches an `oninput` listener whether or not the input is disabled, and an untrusted pointer event never drives a range thumb, so neither direction of a synthetic-drag experiment discriminates. `pointer-events:none` on the disabled input is the second barrier and it **is** measurable, by asking the document what is at the slider's own centre: `row-label-scale` when off, `label-scale` when on. Fourth time a measurement technique, not the code, has been the thing that was wrong. | FEAT-05, FEAT-07 | M | **Closed 2026-09-22** (v3.1.0-P39) | 2026-09-22 |
| TD-147 | The Remarks row control was one button that relabelled itself to the action it would perform, which had to be read as an ACTION while every other segmented control in the same panel reads as a STATE. Now `Row Comments field` with a `Show` / `Hide` pair, driven by `setRemarksVisible(on)` which takes the value rather than flipping, so clicking the already active half is a no-op instead of turning the field off. Both halves are written by `applyMarkerLabelState()`. | FEAT-07 | L | **Closed 2026-09-22** (v3.1.0-P39) | 2026-09-22 |
| TD-148 | The baseline overlay toggle moved from the View Controls panel to the report heading, beside the View toggle it depends on: the overlay only means anything in the Update view. **Moved, not duplicated**, since two controls for one flag is the shape this file has drifted on four times. Its explanatory note became the control's tooltip, written by the same `updateViewToggleUI()` that already owned the disabled state, so the text exists in exactly one place. | FEAT-07, FEAT-09 | M | **Closed 2026-09-22** (v3.1.0-P39) | 2026-09-22 |
| TD-149 | The settings drawer's action bar moved from a sticky footer under the source and import tables to a block above the tabs, so the first thing read is the outcome rather than the machinery. Three exports right anchored in CSV / JSON / Save as new dashboard order, with Clear all comments on its own line underneath where a mis-aimed click cannot reach it. Not sticky any more: `.sd-hd` is itself sticky at `top:0` and there is no measured header height to offset against, so two sticky elements would simply overlap. The drawer's bottom padding came back with the move. `p29_check`'s sticky-footer assertion was rewritten to the new contract and given teeth it did not have, its companion check having been able to pass vacuously whenever the open tab was too short to scroll. | FEAT-07 | M | **Closed 2026-09-22** (v3.1.0-P39) | 2026-09-22 |
| TD-150 | The filter row was one long wrapping strip, so at any width between "all on one line" and "one per line" the groups broke wherever they happened to land. Two containers at `flex:1 1 340px` now hold their own groups and wrap as whole columns, giving the bar two shapes instead of a continuum of them: WHAT narrows the rows on the left, WHEN narrows the columns on the right, with a rule between the week filter and the date range and the summary line full width under both. The basis is a flex basis rather than a media query, so the break happens when the content stops fitting instead of at a width someone typed. Asserted as "side by side XOR cleanly stacked", with which one decided by measured room. The duplicate "Title contains" field the report asked to remove was already gone; the reporting build was a pre-P36 one. | FEAT-06, FEAT-07 | M | **Closed 2026-09-22** (v3.1.0-P39) | 2026-09-22 |
| TD-151 | **"Predecessors and dependencies are not displaying" is REFUTED on the current build.** Measured from the state the board opens in, driving only the real controls: 0 lines before, **675** in the DOM and 132 on screen with both kinds switched on, 478 with the reported date range applied, still 478 after a rerender, back to 675 when the range is cleared, 0 again when switched off, at both 390 and 1440. 143 of the milestones carry dependency data. The screenshot the report came with is a **pre-P36 build** (separate header icon buttons, a "Title contains" field that no longer exists, `Title col: 286px`) and shows the Dependencies row's **All off** button active, which is the state that draws nothing. Asserted permanently as part of TEST-41 rather than fixed, so a real regression cannot hide behind "that was already broken". Left open pending the user's own look at a P39 build, with the state they see it in. | FEAT-05 | M | Open, refuted by measurement | 2026-09-22 |
| TD-152 | `#dep-line-layer`'s computed visibility depends on whether `onSizeSliderDrag` ran since the last `drawDepLines`: hidden on a board nothing has touched, visible on one that has drawn and cleared, because `setWkWidth` reaches the drag-hide at first paint and only `drawDepLines` clears it. Both look identical, since an empty `pointer-events:none` SVG paints nothing either way, so TEST-41 records the visibility and asserts on the line count instead. Found while writing that assertion. Nothing reported it and no user-visible difference was found. | FEAT-05 | L | Open | 2026-09-22 |
| TD-153 | **Rows whose milestones crowd each other grow by half again, judged on the markers ON SCREEN.** A row grows when its densest proximity run of visible markers reaches `MS_LEVEL_CYCLE.length`: three is where every band is in use and the labels stack hard against each other, and it is the band count rather than a number picked. Shipped at v3.1.0-P40 reading the row's TOTAL markers, which made row 51 disagree with the report; the user chose the visible-column rule and it landed at P41. Labels that are off the board collide with nothing, so a row whose crowd the date range is hiding should read like any other row. Measured on the reference board unfiltered: 12 of 159 rows grow, 34.3px to 51px, and all 159 agree with the rule. Under the report's own range, opening at the week ending 06-Sep (column 15): row 18 carries one marker and stays plain; rows 37 (cols 15,16,17,19) and 46 (cols 18,19,19,20) keep four visible each and grow; **row 51 (cols 13,14,15,17) keeps two and stays plain**, which is the reported behaviour. Two mechanics worth keeping. `isDenseRunCols()` is the ONE run cut, called by the renderer that applies the growth and by the filter pass that checks it, because two copies of a run cut is the drift shape this file keeps paying for. And the height cannot be changed in place: `--mdx`/`--mdy` are computed from it at render time, so `applyFilter()` asks whether the rendered heights still match the visible columns and owes a rebuild when they do not, at BOTH of its exits. It terminates because `rerender()` ends by calling `applyFilter()`, which by then agrees with what it just built. | FEAT-05 | **H** | **Closed 2026-09-23** (v3.1.0-P41) | 2026-09-23 |
| TD-154 | The filter bar became three full-width rows, one per question: WHAT (name, band, source, Activity IDs), HOW (the new critical-path set) and WHEN (week on the left, date range anchored right). The two columns P39 shipped had nowhere to put a third question. The critical set is its own ruled container because it filters on a different property of the data, a milestone's state and float, from the rows either side, which filter on identity and on time. Status is a chip group rather than a `<select multiple>`, which needs a modifier key nobody discovers, and **an empty selection means no constraint, which is deliberately not the same as all five selected**; asserted directly, since all five selected must show the same board as none. Float takes an operator, a value and a unit, with one-click presets for the two bands the request names, and converts weeks to days so there is a single unit downstream. A milestone with no recorded float satisfies neither direction: an unknown reported as critical is the kind of wrong that reaches a client. Collapsed, the heading carries a one-click expand control, shown only while the bar is collapsed. | FEAT-06, FEAT-07 | **H** | **Closed 2026-09-22** (v3.1.0-P40) | 2026-09-22 |
| TD-155 | The settings drawer's action bar went back to one row at the user's request: the two exports spread across the left, Save as new dashboard and Clear all comments anchored together on the right. This reverses the P39 arrangement, which put the destructive action on its own line so a mis-aimed click could not reach it; it is still last in the row and still carries its danger colour. Four buttons do not fit one line in a 360px drawer, so the row wraps, and the right-hand pair carries `margin-left:auto` as well as facing a grower: measured, it started 71px short of the edge because on its own line it had no grower to push it. | FEAT-07 | M | **Closed 2026-09-22** (v3.1.0-P40) | 2026-09-22 |
| TD-156 | **The exported CSV could not tell the schedule's figure from a person's.** Progress % and Status carried the EFFECTIVE value, the schedule's own unless an override existed, with nothing in the file saying which. A report that cannot distinguish "the schedule says 40%" from "someone typed 40%" is the wrong report to send to a client. Two named blocks now: `CSV_BASE_HEADER` holds only the schedule's own values and never moves with an annotation, `CSV_ENTERED_HEADER` holds the annotation layer, blank where nothing was entered. Overrides are keyed per milestone and a row can hold several, so a row with more than one entry lists them as `ID: value` pairs rather than picking one. `exportCSV()` was split from `buildCsvRows()` so a check can assert the CONTENT rather than only the headers, which is the thing this change is about. | FEAT-13 | **H** | **Closed 2026-09-22** (v3.1.0-P40) | 2026-09-22 |
| TD-157 | **Add a milestone**, from a header button (its own row under a `User Defined Milestones` band) or a double click on a week cell (that row, that week). A milestone a person adds is not schedule data, so it lives in `USER_MILESTONES`/`USER_ROWS` beside the comment and override stores, round-trips through publish, the model export and selective import, and is MERGED into TASKS/MILESTONES at render time rather than written into them. The merge sits at the top of `renderRows()`, the one function that builds rows, and is idempotent: TASKS/MILESTONES are re-sliced in three places and a fourth would be easy to add, so a merge that repairs itself on the next rebuild cannot be defeated by one being missed. Activity IDs are `USR-NNN`, numbered from the highest already present so a re-import cannot reuse a key. **No recorded convention for user-added IDs was found**, so this one was chosen to match the `PREFIX-NNN` shape every other ID on the board uses; open to change. Weight 0 on purpose: a weighted addition would silently move every percentage on the board. A blank name and a colliding Activity ID are both refused rather than silently suffixed, because the user typed the ID. One delegated `dblclick` listener, not a handler per cell: the reference board is 159 rows by 39 columns. | FEAT-05, FEAT-09 | **H** | **Closed 2026-09-22** (v3.1.0-P40) | 2026-09-22 |
| TD-158 | One dependency line of 360 is degenerate, about a quarter of a pixel long at a legitimate board position, drawn as its own arrowhead. Introduced by the TD-153 row growth: isolated by rendering the same board with the growth factor at 1.0, which gives zero. It is two connected markers that landed within a pixel of each other, not the line-collapse `p30_check` section 8 exists for, so that check asserts zero lines AT THE BOARD ORIGIN, which is the failure that makes the feature look broken, and bounds the degenerate count rather than asserting it at zero. Cosmetically invisible. Unchanged by the P41 move to visible-column growth, which alters which rows grow but not how a grown row positions its markers. | FEAT-05 | L | Open | 2026-09-23 |
| TD-159 | **The growth recompute was wired into two of its three entry points.** `applyFilter()` got it at both exits; `clearFilter()` did not, and `clearFilter()` un-hides every row and column ITSELF rather than routing through `applyFilter()`. So Remove all filters put the columns back and left rows that should have grown at the short height. Found by `p33_check`'s "only the clone row grew" assertion, which read 4 of 105 rows changed: the clone row correctly, plus SNIP-115, SNIP-159 and SNIP-242 going 34.3px to 51px, which is the growth they were owed earlier. **Fourth instance of this trap, and the comment three lines above the missing line was already describing it** for the dependency-line redraw that hit the same gap at TD-106. The check's failure message named only a count, so it was made to name the rows as well. `p40_check` now asserts the entry-point count at source and drives the Remove all filters route at runtime. | FEAT-06 | **H** | **Closed 2026-09-23** (v3.1.0-P41) | 2026-09-23 |
| TD-160 | **A milestone the upload records as complete and one a person just marked off rendered the same black.** The status vocabulary is now one list, Critical / At risk / On track / Done / Complete / N/A, shared by the icons, the chips, the row health picker and the card, with `STATE_LABELS` and `HEALTH_LABELS` as the single source of each name. The colour defect was NOT where the report suggested: `--color-icon-done` was already ink and the card's `mh-2` dot was already green. `effectiveState()` was mapping a person's override of 2 onto the schedule's own `DONE`, so both kinds of finished collapsed onto one state and one colour. A new `DONEUSER` state takes the override instead, which is the distinction the request asks for: **black is what the upload recorded, green is what someone did in this update**. Measured on the rendered board, not read from the CSS: Complete `rgb(23,25,28)`, Done `rgb(31,157,85)`, with a negative control proving that clearing the mark returns the milestone to its schedule state rather than leaving it green. Both carry `credit:1`, so marking something off cannot quietly drop it out of the completed rollup, asserted directly. Two deliberate non-changes. The internal key stayed `DONE` rather than becoming `COMPLETE`, because the embedded P6 seed data literally carries `state:"DONE"` and that is the upload's own vocabulary; the user-facing word lives in `STATE_LABELS`. And health numbers 0 to 3 keep their numbers (3 relabelled Issue / delayed to Critical, 4 added as Done), because renumbering would silently re-colour every published file and exported model already in circulation. `FUTURE` is kept as a seventh state although the request's list does not name it: it is a real schedule state meaning not yet started, and dropping it would change the board. **Complete is not settable by a person** — black is reserved for what the upload records, which is the reading the request supports. | FEAT-05, FEAT-07 | **H** | **Closed 2026-09-23** (v3.1.0-P42) | 2026-09-23 |
| TD-161 | **`theme_check.py`'s `constant` probes assert nothing on their own.** A probe that toggles when it was expected to be constant is reported under TOGGLING correctly and the script still exits 0, by design (the comment calls it "merely informational"), so the only defect a constant probe can catch is none. Demonstrated while adding the two new `s-doneuser` probes: pointing one at a class that does not exist made it inherit the ink colour, which toggles, and it passed. The `toggle` direction is sound and is what the tool was built for. Rather than change the semantics of eighteen existing constant probes inside an unrelated partial, the real assertion was put where the colours are already read for what they are, in `p42_check`: with the theme flipped to dark, the person-marked green must be the identical `rgb(31,157,85)` it is in light, the upload's ink must follow the theme to `rgb(232,238,248)`, and the two must still differ. Making a constant probe fail when it starts toggling is the fix, and it belongs in its own pass with the eighteen re-checked. | FEAT-07 | M | Open | 2026-09-23 |
| TD-05 | Whether `main` should carry a branch index. | FEAT-15 | L | **Closed 2026-09-10** — moot. `main-projects-hub` is now the default branch and carries the index; `main` has been deleted. | 2026-09-09 |

---
**Next code task (single line, always current):** Half 2 of the multi-source work, which hangs its compare-overlay kind off `renderMarker()`. Then TD-97, point the four View Controls occurrences of `--color-purple-deep` at `--color-accent-ink` and probe each. Then TD-98/TD-88, give the week header and legend their own light-strip token so the duplicate dark-theme `--color-bg-subtle` can go. That order was set by the user on 2026-09-19 and interrupted five times: by the four P36 defect reports, closed at P37; by the P37 print-preview heading, date-range and view-default requests, closed at P38; by the eight-item View Controls / filter-row batch, closed at P39 with TD-151 refuted and left open for the user's own look; and by the six-item row-growth / critical-filters / CSV / add-milestone batch, closed at P40, with TD-153's row 51 settled at P41 on the user's choice of the visible-column rule and TD-157's ID convention still open with them; and by the status colour convention, closed at P42. The label-clipping re-measure stays deferred with no date against it. Alongside: TD-161 the `constant` probes in `theme_check.py` that cannot fail; TD-141 the shared `zero-stub` class on two marks that mean different things; TD-135 the residual label clip at 390px, needing a design call on what gives way at phone width; TD-129 the refuted SNIP-218/219 report, still pending the user's own look at a P34 or later build; TD-125 the non-monotonic spill; TD-117 the dead bottom of the Row height slider range; then TD-73, TD-67, TD-36, TD-62, TD-28 and TD-33.
